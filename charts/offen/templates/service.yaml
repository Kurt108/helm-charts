apiVersion: v1
kind: Service
metadata:
  name: {{ template "offen.fullname" . }}
  labels:
    app: {{ template "offen.name" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: {{ .Values.service.name }}
  selector:
    app: {{ template "offen.name" . }}
    release: {{ .Release.Name }}